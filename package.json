{
  "name": "war-table-claude",
  "version": "1.0.0",
  "description": "Five Claude personas debate your toughest decisions across three rounds and deliver a verdict. Unofficial replica of wartable.co that runs on your Claude Pro/Max subscription via the Claude Agent SDK — no API key needed.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "MOCK=1 node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@anthropic-ai/sdk": "^0.111.0",
    "express": "^4.19.2"
  }
}
